services:
  api:
    image: funds-explorer-scraper/api
    build: ../api
    env_file:
    -   .env.api
    ports:
    -   ${API_SERVER_PORT}:80
  web:
    image: funds-explorer-scraper/web
    build: ../web
    env_file:
    -   .env.web
    ports:
    -   ${WEB_SERVER_PORT}:80
